# Test Server

This directory contains a Dockerfile to setup a POP3 server for testing purposes.

## Setup

```bash
# Build the test server
./build

# Run the test server
./run
```

The test server is available at `localhost:110`.

## References 

- https://doc.dovecot.org
